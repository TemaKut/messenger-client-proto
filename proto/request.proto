syntax = "proto3";

option go_package = "gen/go";

message Request {
  uint64 id = 1;

  reserved 2 to 10;

  oneof data {
    AuthRequest auth = 11;
  }
}

message AuthRequest {
  string username = 1;
  string password = 2;
}